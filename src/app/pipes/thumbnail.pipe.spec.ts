import { ThumbnailPipePipe } from './thumbnail.pipe';

describe('ThumbnailPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ThumbnailPipePipe();
    expect(pipe).toBeTruthy();
  });
});
